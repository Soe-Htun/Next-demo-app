import React from 'react'

// () means route groups, you can put name within (), 
// you can test in browser like /login, not auth/login
export default function Login() {
  return (
    <div>Login</div>
  )
}
